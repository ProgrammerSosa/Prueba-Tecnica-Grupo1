
export const seedInitialData = async () => {
    try {
        console.log('Seed data placeholder - no data to seed yet');
    } catch (error) {
        console.error('Error al ejecutar seed:', error);
    }
};
